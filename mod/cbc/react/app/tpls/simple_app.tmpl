import {BrowserRouter, Routes, Route} from 'react-router-dom';
import {Schemas} from "./onepage/SchemasViewer";
import {Entities} from "./onepage/EntitiesViewer";

function SimpleApp(){
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Schemas />} />
        <Route path="/entity" element={<Entities />} />
      </Routes>
    </BrowserRouter>
    
  )
}

export {SimpleApp}
